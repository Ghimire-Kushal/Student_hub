export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 p-8">
      <h1 className="text-4xl font-light tracking-tight">Study Tracker</h1>
      <p className="text-ink-muted font-mono text-sm">
        Your academic progress, organised.
      </p>
      <div className="mt-4 rounded-lg border border-border bg-white/60 p-6 text-sm text-ink-muted">
        Step 1 complete — fonts, tokens &amp; layout ready.
      </div>
    </main>
  );
}
